export const COOKIE_NAMES = {
  REFRESH_TOKEN: "pulseline_refresh_token",
};

export const OTP_LENGTH = 6;
export const OTP_EXPIRY_MINUTES = 10;
export const OTP_MAX_ATTEMPTS = 5;

export const APPOINTMENT_SLOT_MINUTES = 30;
